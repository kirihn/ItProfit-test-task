export function correctFormLabel(form, errors){

    if(errors.name != ""){
        form.nameLabel = "* " + errors.name;
    }
    else{
        nameLabel = "";
    }

    if(errors.name != ""){
        nameLabel = "* " + errors.name;
    }
    else{
        nameLabel = "";
    }

    if(errors.name != ""){
        nameLabel = "* " + errors.name;
    }
    else{
        nameLabel = "";
    }

    if(errors.name != ""){
        nameLabel = "* " + errors.name;
    }
    else{
        nameLabel = "";
    }
}